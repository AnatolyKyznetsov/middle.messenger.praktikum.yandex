{{#> page title="Профиль - изменить пароль"}}
    {{#> shape buttonBack="/profile" className=""}}
        {{#> form buttonLable="Сохранить" className=""}}
            {{> label name="oldPassword" text="Старый пароль" type="password" attrs="required"}}
            {{> label name="newPassword" text="Новый пароль" type="password" attrs="required"}}
            {{> label text="Новый пароль (ещё раз)" type="password" attrs="required"}}
        {{/form}}
    {{/shape}}
{{/page}}
