{{#> page title="Вход"}}
    {{#> shape className=""}}
        {{> title-main text="Вход" className="shape__title shape__title_big"}}
        {{#> form buttonLable="Войти" className=""}}
            {{> label name="login" text="Логин" attrs="required"}}
            {{> label name="password" text="Пароль" type="password" attrs="required"}} 
        {{/form}}
        {{> link url="/" text="Ещё не зарегистрированы?" className="shape__link"}}
    {{/shape}}
{{/page}}
